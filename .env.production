# Eingecheckte Produktions-Vorgabe für die Benachrichtigungen (Bauabschnitt 8).
#
# Beide Werte sind ÖFFENTLICH und dürfen im Repo liegen:
#   - der VAPID-Public-Key ist ohnehin im ausgelieferten Frontend sichtbar,
#   - die Worker-URL ist eine öffentliche Adresse.
# Der private VAPID-Schlüssel liegt ausschliesslich als Cloudflare-Worker-Secret
# und niemals hier.
#
# Vite lädt diese Datei bei `vite build` automatisch, damit jeder Deploy die
# Erinnerungen kennt, ohne dass die Werte von Hand nachgetragen werden müssen.

VITE_VAPID_PUBLIC_KEY=BElSXXHOZOb4WQ6SPwHxPcmnmDkpik0mSFLyuibWt8TxKwTC12jN6sQJO9rYQ3eMHpwAzJsWsKSfk2m__ALL3w0
VITE_WORKER_URL=https://gebet-benachrichtigung.zyklustracker.workers.dev
