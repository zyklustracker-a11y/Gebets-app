node_modules/
dist/
*.local
.DS_Store
*.tsbuildinfo

# Lokaler Firebase-Hosting-Deploy-Cache (wird beim Deploy erzeugt).
.firebase/

# Lokaler Zwischenspeicher von wrangler (entsteht bei jedem Worker-Deploy).
.wrangler/

# Lokale Konfiguration mit Schlüsseln — nur Beispiel- und öffentliche
# Produktions-Vorgabe gehören ins Repo.
.env
.env.*
!.env.example
!.env.production
